import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  iosContainer: {
    flex: 1,
  },
  container: {
    height: '100%',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'space-around',
    marginHorizontal: 8,
  },
});
